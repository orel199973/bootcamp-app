apiVersion: v1
kind: Service
metadata:
    name: "kubernetes-b050"
    labels:
        app: "kubernetes-b050"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "kubernetes-b050"
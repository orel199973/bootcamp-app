apiVersion : apps/v1
kind: Deployment
metadata:
  name: "kubernetes-2279"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "kubernetes-2279"
  template:
    metadata:
      labels:
        app: "kubernetes-2279"
    spec:
      containers:
        - name: "kubernetes-2279"
          image: "dockerregg.azurecr.io/kubernetes"
          ports:
          - containerPort: 8080